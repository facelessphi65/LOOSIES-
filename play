<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>LOOSIES!</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      width: 100vw;
      display: flex;
      justify-content: center;
      align-items: center;
      background: black url('loosies.jpg') no-repeat center center fixed;
      background-size: contain;
      font-family: sans-serif;
    }

    .container {
      text-align: center;
      padding: 1rem;
      background-color: #222; /* dark grey for player box */
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0,0,0,0.4);
      animation: float 2s ease-in-out infinite;
      margin-top: 180px;
      transform: translateY(30px);
      width: 80vw;
      max-width: 500px;
    }

    @keyframes float {
      0% { transform: translateY(30px); }
      50% { transform: translateY(20px); }
      100% { transform: translateY(30px); }
    }

    h1 {
      color: white; /* white title */
      font-size: 2rem;
      text-align: center; /* centered title */
      margin: 0;
    }

    audio {
      background-color: #333; /* slightly dark grey */
      border-radius: 8px;
      padding: 0.5rem;
      width: 100%;
      max-width: 90vw;
      margin-top: 1rem;
      outline: none;
    }

    .controls {
      margin-top: 1rem;
    }

    button {
      padding: 0.5rem 1rem;
      margin: 0 0.5rem;
      background-color: #111; /* very dark grey */
      color: white; /* white text/icons */
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 1rem;
    }

    button:hover {
      background-color: #444; /* lighter on hover */
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>LOOSIES! 💀</h1> <!-- skull emoji -->

    <audio id="player" controls>
      <source id="trackSource" src="" type="audio/ogg" />
      Your browser does not support the audio element.
    </audio>

    <div class="controls">
      <button onclick="prevTrack()">⏮️ Prev</button>
      <button onclick="nextTrack()">Next ⏭️</button>
    </div>
  </div>

  <script>
    const tracks = [
      { src: "music/track1.ogg", title: "PRUDENTIAL!" },
      { src: "music/track2.ogg", title: "DUMBO!" },
      { src: "music/track3.ogg", title: "ANTIDEPRESSANTS!" },
      { src: "music/track4.ogg", title: "iNFORMANT! interlude" },
      { src: "music/track5.ogg", title: "nao falar!" }
    ];

    let currentTrack = -1;
    const player = document.getElementById("player");
    const source = document.getElementById("trackSource");

    function loadTrack(index) {
      if (index < 0) index = tracks.length - 1;
      if (index >= tracks.length) index = 0;
      currentTrack = index;
      source.src = tracks[currentTrack].src;
      player.load();
      player.play();
    }

    function prevTrack() {
      if (currentTrack === -1) loadTrack(0);
      else loadTrack(currentTrack - 1);
    }

    function nextTrack() {
      if (currentTrack === -1) loadTrack(0);
      else loadTrack(currentTrack + 1);
    }

    player.addEventListener('ended', () => {
      nextTrack();
    });
  </script>
</body>
</html>
